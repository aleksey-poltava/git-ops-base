apiVersion: apps/v1
kind: Deployment
metadata:
  name: sample-alex-app
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: sample-alex-app
  template:
    metadata:
      labels:
        app: sample-alex-app
    spec:
      containers:
      - command:
        - /app/sample-app
        image: gitopsbook/sample-app:v0.1
        name: sample-app
        ports:
        - containerPort: 8080